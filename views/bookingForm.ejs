<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Booking</title>
</head>
<body>
  <%- include('partials/navbar') %>  
  
  <h1>Book <%= restaurant.name %></h1>
  <h2><%= user.first_name %> <%= user.last_name %></h2>

  <form action="/restaurants/booking/<%= restaurant.id %> " method="post">
    <label for="">Guest Id</label>
    <br>
    <input readonly type="text" name="UserId" id="UserId" value="<%= user.id %>"> <br>
    
    <label for="">Name</label>
    <br>
    <input type="text" name="name" id="name" value="<%= restaurant.name %>"> <br>
    <label for="">Date</label>
    <br>
    <input type='date' name="date" id="date"> 
    <br>

    <label for="">Time</label>
    <br>
    <select name="time" id="time">
      <option value="">Select Time</option>
      <% for( let i = restaurant.opening_hours; i < restaurant.closing_hours; i++ ) { %>
        <option value="<%= restaurant.generateAmPm(i) %>"><%= restaurant.generateAmPm(i) %> </option>
      <% } %>
    </select>
    <br> <br> 
    <input type="submit" value="Booking"> 
  </form>
</body>
</html>